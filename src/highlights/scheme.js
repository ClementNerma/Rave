// Enable strict mode
"use strict";

// Export the scheme
scheme = {
  constants: {
    // Visual Studio Code specific variables
    execPath: '${execPath}',
    workspaceRoot: '${workspaceRoot}'
  },

  patterns: []
};
